import React from "react";

export const Loading = () => {
  return (
    <div
      style={{
        margin: "0 auto",
        marginTop: 40 + "px",
        marginBottom: 60 + "px",
        textAlign: "center",
      }}
    >
      Loading
    </div>
  );
};
